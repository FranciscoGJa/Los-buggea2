<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.web.WebView?>

<VBox xmlns="http://javafx.com/javafx/11.0.1" xmlns:fx="http://javafx.com/fxml/1"> 
    <Label text="Técnicas de Relajación"  />

    <!-- Filtros y controles superiores -->
    <HBox spacing="15" alignment="CENTER_LEFT">
        <Label text="Filtrar por tipo:" style="-fx-font-weight: bold;"/>
        <ComboBox fx:id="comboTipo" prefWidth="150"/>
        
        <Region HBox.hgrow="ALWAYS"/>
        
        <Button text="Limpiar" onAction="#onLimpiarSeleccion" />
        <Button text="Recargar" onAction="#onRecargar" />
        
        <Label text="Zoom:" style="-fx-font-weight: bold;"/>
        <ChoiceBox fx:id="zoomBox" prefWidth="80"/>
        
        <Button text="← Anterior" onAction="#onPrev" />
        <Button text="Siguiente →" onAction="#onNext" />
        <Button text="Acerca de" onAction="#onAcercaDe" />
    </HBox>

    <!-- Contenido principal -->
    <HBox spacing="15" VBox.vgrow="ALWAYS">
        <!-- Panel izquierdo - Lista de PDFs -->
        <VBox spacing="10" prefWidth="300" style="-fx-background-color: white; -fx-border-color: #bdc3c7; -fx-border-radius: 5; -fx-padding: 10;">
            <Label text="Técnicas Disponibles" style="-fx-font-size: 14px; -fx-font-weight: bold; -fx-text-fill: #34495e;"/>
            
            <TableView fx:id="tablaPDFs" VBox.vgrow="ALWAYS" style="-fx-border-color: #ecf0f1;">
                <columns>
                    <TableColumn fx:id="colNombre" text="Nombre" prefWidth="180"/>
                    <TableColumn fx:id="colTipo" text="Tipo" prefWidth="100"/>
                </columns>
                <columnResizePolicy>
                    <TableView fx:constant="CONSTRAINED_RESIZE_POLICY"/>
                </columnResizePolicy>
            </TableView>
        </VBox>

        <!-- Panel derecho - Visor e información -->
        <VBox spacing="10" VBox.vgrow="ALWAYS" HBox.hgrow="ALWAYS">
            <!-- Información del documento -->
            <VBox style="-fx-background-color: #e8f4f8; -fx-border-color: #3498db; -fx-border-radius: 5; -fx-padding: 10;">
                <HBox spacing="10" alignment="CENTER_LEFT">
                    <Label fx:id="pageLabel" text="Selecciona un documento" style="-fx-font-weight: bold; -fx-text-fill: #2980b9;"/>
                    <Region HBox.hgrow="ALWAYS"/>
                </HBox>
                <Label fx:id="labelInfo" text="Selecciona una técnica de la lista para verla" wrapText="true" style="-fx-text-fill: #2c3e50;"/>
            </VBox>

            <!-- Visor PDF -->
            <VBox VBox.vgrow="ALWAYS" HBox.hgrow="ALWAYS" style="-fx-border-color: #7f8c8d; -fx-border-radius: 3;">
                <WebView fx:id="webView" VBox.vgrow="ALWAYS" HBox.hgrow="ALWAYS"/>
            </VBox>
        </VBox>
    </HBox>

    <!-- Pie de página -->
    <HBox spacing="10" alignment="CENTER" style="-fx-padding: 5;">
        <Label text="Sistema de Técnicas de Relajación - Versión 1.0" style="-fx-text-fill: #7f8c8d; -fx-font-style: italic;"/>
    </HBox>

</VBox>